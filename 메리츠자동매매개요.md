# 메리츠 자동매매 프로그램

## 1. 개요

메리츠증권 HTS 화면을 Win32 API + pyautogui로 자동 조작하여, 구글 시트에 정의된 그리드 매매 전략대로 자동 매매를 수행하는 프로그램이다.

## 2. 시스템 구성

| 구성요소 | 설명 |
|---------|------|
| 언어 | Python (32bit) |
| HTS | 메리츠증권 iMERITZ XII (`imeritz.exe`) |
| HTS 조작 | Win32 API (`ctypes`) + `pyautogui` |
| 매매 전략 | 구글 시트 (gspread + oauth2client) |
| 알림 | 텔레그램 봇 (requests) |
| 설정 GUI | PyQt5 (`config_gui.py`) |
| 빌드 | PyInstaller (`build.bat` + `build.spec`) |
| 실행 | 관리자 권한 자동 승격 (`main.py`) |

## 3. 파일 구조

```
메리츠자동매매/
├── main.py              # 진입점 (관리자 권한 승격 후 AutoTrader 실행)
├── trader.py            # AutoTrader 클래스 (로그인 → 매매 루프)
├── meritz_hts.py        # HTS 조작 (실행, 로그인, 화면 열기, 현재가/보유량 조회, 매수/매도/미체결취소)
├── google_sheet.py      # 구글 시트 읽기/쓰기 (batch_get으로 API 최소화)
├── telegram_notify.py   # 텔레그램 알림 (requests 기반)
├── config.py            # config.json 로드 및 검증 + 구글 시트에서 텔레그램 설정 읽기
├── config_gui.py        # PyQt5 환경설정 GUI
├── login_32bit.py       # 32bit Python 전용 로그인 스크립트
├── extract_log.py       # 로그 추출 유틸리티
├── find_alert.py        # 안내창/팝업 디버깅 유틸리티
├── config.json          # 설정 파일
├── credentials.json     # 구글 서비스 계정 키
├── requirements.txt     # 의존성
├── build.bat            # PyInstaller 빌드 배치
├── build.spec           # PyInstaller 빌드 스펙
└── run_admin.bat        # 관리자 권한 실행 배치
```

## 4. 설정 (config.json)

```json
{
    "hts_path": "C:\\메리츠증권\\iMERITZ XII\\Main\\imeritz.exe",
    "accounts": [
        {
            "cert_name": "<이름>",
            "cert_index": 4,
            "cert_password": "<비밀번호>"
        }
    ],
    "google_sheet": {
        "credentials_file": "credentials.json",
        "spreadsheet_id": "<시트ID>"
    },
    "check_interval_minutes": 3,
    "speed_multiplier": 1.0,
    "sheet_names": ["<시트명>"]
}
```

- `accounts`: 최대 4개까지 등록 가능
- `cert_index`: 인증서 선택 창에서 위에서부터 0-based 인덱스
- `check_interval_minutes`: 1~30분
- `speed_multiplier`: 느린 PC에서 대기 시간 배수 (1.0 이상, 느린 PC는 2~3)
- `sheet_names`: 매매 대상 구글 시트 워크시트 이름 목록

## 5. 동작 흐름

### 5.1 실행 및 로그인

1. `main.py` → 관리자 권한 자동 승격
2. `hts.launch()` → `imeritz.exe` 실행 (12초 × speed_multiplier 대기)
3. `hts.login()` → "인증서 선택" 창 탐색 (최대 60초)
   - SysListView32에서 `cert_index`번째 항목을 키보드(Home + Down)로 선택
   - 비밀번호 Edit(폭 140~300px)에 입력
   - "확인" 버튼 클릭 (60초 × speed_multiplier 대기)

### 5.2 매매 루프

1. `sheet_names`에서 첫 번째 워크시트 선택
2. `batch_get` 1회로 매매 대상 정보(E4:K18) + 티어 정보(V5:AC열) 동시 조회
3. 종목별 처리:
   - HTS 창 찾기 (`hts_name`으로 윈도우 탐색)
   - **미체결 일괄취소**: 2303 화면에서 해당 종목 미체결 주문 전체 취소
   - 2020 화면 열기 (서브창 닫기 → 화면번호 입력)
   - **계좌 선택**: 드롭다운 ListView에서 계좌번호 매칭
   - **현재가 조회**: 매도 탭 → 종목코드 입력(WM_SETTEXT + 검증) → 자동(현재가) 체크 → 가격 Edit 읽기
   - **보유량 조회**: "가" 버튼 클릭 → 안내창 뜨면 0, 아니면 수량 Edit 읽기
   - **티어 매칭**: 잔고량 기준으로 현재 티어 결정
   - **매매 신호 판단**: 현재가와 매수가/매도가 중간값 비교
   - **주문 실행**: 매수(F9) 또는 매도(F12) → 확인창 → 주문확인 → 안내창 처리
   - 구글 시트에 현재가(K8), 보유량(K10), 현재 티어(K6), 최종 업데이트(K4) 업데이트
   - 매매 후 2303 화면(미체결 조회) 열기

### 5.3 매매 전략 (그리드 매매)

- 잔고량 기준으로 현재 티어를 매칭
- 현재가를 해당 티어의 매수가와 매도가의 **중간값**과 비교
  - 현재가 < 중간값 → 매수 신호
  - 현재가 > 중간값 → 매도 신호
- 0티어: 매수가 이하일 때만 매수
- 마지막 티어: 매도만 시도
- **잔고 불일치 처리**: 실제 잔고와 시트 목표 잔고가 다르면 차이분을 매매 수량에 반영
  - 매도 시: 시트 매도량 + 초과분
  - 매수 시: 시트 매수량 - 부족분
- **주문 가격**: 매수 시 매수가 × 0.9, 매도 시 매도가 × 1.1 (체결 확률 향상)

## 6. 구글 시트 셀 맵

### 설정 영역 (E열)

| 셀 | 항목 |
|----|------|
| E4 | HTS 창 이름 |
| E6 | 계좌번호 |
| E8 | 종목코드 |
| E10 | 투자금 |
| E12 | 티어 분할 수 |
| E14 | 1티어(USD) |
| E16 | 1티어 갱신 여부 (TRUE/FALSE) |
| E18 | 매수 한도 |
| E23 | 텔레그램 Chat ID |
| E25 | 텔레그램 Bot Token |

### 상태 영역 (K열)

| 셀 | 항목 |
|----|------|
| K4 | 최종 업데이트 시각 |
| K6 | 현재 티어 |
| K8 | 현재가 |
| K10 | 보유량 |
| K12 | 수량 차이 |
| K14 | 매수 횟수 |
| K16 | 매도 횟수 |

### 프로그램 영역 (V~AC열, 행5부터)

| 열 | 항목 |
|----|------|
| V | 티어 |
| W | 잔고량 |
| X | 투자금 |
| Y | 티어평단 |
| Z | 매수(가) |
| AA | 매수(량) |
| AB | 매도(가) |
| AC | 매도(량) |

## 7. HTS 화면 조작 상세

### 화면번호 입력

- HTS 메인 창의 Edit 컨트롤 (39x12px) 탐색 → WM_SETTEXT로 설정 → 클릭 → Enter
- 재시도 로직 포함 (open_screen_with_retry, 최대 2회)

### 2020 화면 (해외주식 주문)

- **계좌 선택**: Edit(60~125px, 16px) 클릭 → 드롭다운 ListView에서 계좌번호 매칭 (재시도 포함)
- **종목코드 입력**: Edit (폭 80~86, 높이 14~18) → WM_SETTEXT + Enter → 입력값 검증 (최대 3회 재시도)
- **현재가 읽기**: "자동(현재가)" 버튼 위의 Edit (88x20)에서 WM_GETTEXT로 읽기
- **보유량 읽기**: "가" 버튼(20x20) 클릭 → "수량" 라벨 근처 Edit (88x20)에서 읽기
- **매수 주문**: 매수 탭 → 종목코드/수량/가격 입력 → F9 → 확인 → 매수확인 → 안내창
- **매도 주문**: 매도 탭 → 종목코드/수량/가격 입력 → F12 → 확인 → 매도확인 → 안내창

### 2303 화면 (미체결 관리)

- **미체결 일괄취소**: 계좌 선택 → "종목" 필터 → 종목코드 입력(71x16 Edit) → "전체" 선택 → 조회 → 헤더 체크박스 → 일괄취소
- 매매 후 자동으로 2303 화면 열어 미체결 현황 표시

### 팝업 처리

- `_close_popups()`: 안내/확인/오류/알림 팝업 자동 닫기
- `_find_popup()`: 타이틀 정확 일치 + visible 조건으로 팝업 탐색
- 안내창 메시지를 텔레그램으로 전송

## 8. 텔레그램 알림

- 구글 시트(E23, E25)에서 Chat ID와 Bot Token을 읽어 초기화
- 매수/매도 주문 시 주문 정보 + HTS 안내창 메시지 전송
- 잔고 읽기 실패, 현재가 읽기 실패 시 경고 알림
- 오류 발생 시 에러 알림
- 재시도 로직 포함 (최대 3회, 지수 백오프)

## 9. 환경설정 GUI (config_gui.py)

- PyQt5 기반 GUI로 config.json 편집
- HTS 경로, 키움 계정, 구글 시트, 시트 이름, 확인 주기, 속도 배수 설정
- "바로 실행", "저장", "저장 후 실행" 버튼
- PyInstaller로 `환경설정.exe`로 빌드 가능

## 10. 의존성

```
certifi, cffi, charset-normalizer, comtypes, cryptography
google-auth, google-auth-oauthlib, gspread, httplib2
oauth2client, oauthlib
pillow, pyautogui, pyperclip, pywinauto
PyQt5, PyQt5-Qt5, PyQt5_sip
pytesseract, pywin32
requests, schedule
```

## 11. 실행 방법

```bat
:: 관리자 권한으로 실행 (자동 승격)
python main.py

:: 환경설정 GUI
python config_gui.py

:: 또는 배치 파일
run_admin.bat

:: 빌드
build.bat
```

## 12. 로깅

- `trader.log`에 RotatingFileHandler로 기록 (최대 1MB, 백업 1개)
- 콘솔 + 파일 동시 출력
- 매매 신호, 주문 상세, 안내창 메시지, 오류 등 상세 로깅

## 13. 현재 상태

- [x] HTS 실행 및 인증서 로그인
- [x] 2020 화면 열기 (재시도 포함)
- [x] 계좌번호 선택 (드롭다운)
- [x] 현재가 조회
- [x] 보유량 조회
- [x] 구글 시트 읽기/쓰기 (batch_get 최적화)
- [x] 티어 매칭 및 매매 신호 판단
- [x] 실제 매수/매도 주문 실행 (F9/F12)
- [x] 잔고 불일치 시 수량 보정
- [x] 미체결 일괄취소 (2303 화면)
- [x] 텔레그램 알림 연동
- [x] 스케줄 기반 반복 실행
- [x] 환경설정 GUI (PyQt5)
- [x] PyInstaller 빌드
